/** Published npm package name for the BehalfID CLI (kept out of packages/ so the web app does not import the CLI tree). */
export const CLI_NPM_PACKAGE_NAME = "@behalfid/cli";

export const CLI_NPM_INSTALL_COMMAND = `npm install -g ${CLI_NPM_PACKAGE_NAME}`;
