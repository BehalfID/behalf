/** Current installation state document schema version. */
export const INSTALLATION_STATE_SCHEMA_VERSION = 1 as const;

/** Canonical MCP server name used when registering the BehalfID runtime. */
export const BEHALF_MCP_SERVER_NAME = "behalfid" as const;
